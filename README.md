# Usage 

1. Run `./install.sh` to install the program.       
2. Connect the Kindle to your computer using a micro-USB cable.    
3. Run command `$ kindle-cleaner` to clean up your Kindle directory.      

After install the program, you can run `$ man kindle-cleaner` for more information.     
